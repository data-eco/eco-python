platforms:
  type: 'dict'
  required: true
  keysrules:
    type: 'string'
    regex: '[a-z0-9_]+'
    schema:
      title:
        type: 'string'
        required: true
      assays:
        type: 'list'
        required: true
        schema:
          type: 'string'
