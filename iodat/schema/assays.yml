assays:
  type: 'dict'
  required: true
  keysrules:
    type: 'string'
    regex: '[a-z0-9_]+'
    schema:
      title:
        type: 'string'
        required: true
      type:
        type: 'string'
        required: true
